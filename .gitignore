node_modules
dist
.DS_Store

# env (never commit secrets)
.env
.env.*.local

# backend not needed for frontend deploy
backend/

# WordPress — full Hostinger export is huge; keep only custom theme + plugin (added with git add -f)
wordpress/*.sql
wordpress/public_html/

